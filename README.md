# Cyrax-key-logger
Python keylogger project that demonstrates how keystrokes are captured, stored, and displayed in a controlled environment. Uses Pynput/Keyboard for input capture, Tkinter for GUI, and saves data in text or JSON files. For learning purposes only.
